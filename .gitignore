.DS_Store
**/.DS_Store

# triage skill — config holds the Apps Script webhook secret
.claude/skills/triage/config.local.json
# triage skill — internal session log (not for git history)
.claude/skills/triage/log.local.md

# legacy /triage command (superseded by the triage skill)
.claude/commands/triage.md

# User-specific Claude permissions
.claude/settings.local.json

# temporary pre-regeneration backup of the migration export (Step 0 rollback insurance; deleted after verify)
pathways_migration_june_2026.pre-regen.bak/
# ncv-persona skill — internal run log (not for git history)
.claude/skills/ncv-persona/log.local.md
